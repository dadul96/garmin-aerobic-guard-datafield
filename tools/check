#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_dir"

./tools/check-portable

./tools/garmin build
test -f bin/app.prg
./tools/garmin test
test -f bin/tests.prg
